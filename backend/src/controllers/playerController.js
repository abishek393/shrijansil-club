import Player from "../models/playerModel.js";


export const registerPlayer = async() =>{
    const {fullName, address, dateOfBirth, gender, email, contactNumber, TshirtSize, bloodGroup, emergencyContact, paymentVoucher, authenticateDocument} = req.file;

}